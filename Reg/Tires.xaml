<Window x:Class="Reg.Tires"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Reg"
        mc:Ignorable="d"
        Title="Tires" Height="450" Width="800">
    <Grid>
        <TextBox x:Name="privetik" Text="dfdf"/>
        <DataGrid AutoGenerateColumns="False" x:Name="Goods">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Категория" Binding="{Binding Path=Product.ProductActicle}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
